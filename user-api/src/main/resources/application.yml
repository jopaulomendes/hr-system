server:
  port: ${PORT:8000}
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: user-api
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}